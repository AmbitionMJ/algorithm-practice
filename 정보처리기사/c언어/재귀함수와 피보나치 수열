#include <stdio.h>

int f(int i); // 함수 호출 전에 미리 정의해준다. (프로토타입)

void main() {
    printf("%d %d %d", f(1), f(5), f(-2));  //1 5 1 출력 
}

int f(int i) { 
    if (i <= 2)
        return 1;
    else
        return f(i - 1) + f(i - 2);
}


//f(5)를 호출할 때는 f(4)+f(3)이 되고 f(4)는 f(3)+f(2)를 반환하고 f(3)은 f(2)와 f(1)을 반환
//똑같이 f(3)도 f(2)와 f(1)을 반환하기 때문에 결과적으로 3+2가 됩니다. 

//f(1)과 f(-2)는 if문을 만족하므로 각각 1씩 출력됩니다. 

//결과 1 5 1
